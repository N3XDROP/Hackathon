services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env
    depends_on:
      - chat
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "5173:80"

  chat:
    build: ./chat
    ports:
      - "5000:5000"
